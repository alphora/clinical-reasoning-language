import * as vscode from "vscode";

// Todo 1 scaffold only — no behavior yet.
// Provisioning (MCP config + skill + highlighting settings) lands in todos 3–5.
export function activate(_context: vscode.ExtensionContext): void {
  // intentionally empty
}

export function deactivate(): void {
  // intentionally empty
}
