# Dependency directories
node_modules/
# ...except test fixture node_modules — these are committed fake packages
# exercised by resolver tests; they are not real npm dependencies.
!src/imports/tests/fixtures/**/node_modules/
!src/imports/tests/fixtures/**/node_modules/**

# Build outputs
dist/
build/
lib/
coverage/
src/grammar/generated/
tsconfig.tsbuildinfo
*.tgz

# Debug & log files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE/editor settings (keep settings.json only)
.idea/
*.sublime-project
*.sublime-workspace
.DS_Store

.vscode/*
!.vscode/settings.json

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Test outputs
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# Vibe Tools — extension-managed assets (protocols/, prompts/) +
# Claude-maintained local workflow artifacts (discussions/, retros/).
# Safe to delete to reset the extension; you will lose any local
# consultation transcripts and retros. Promote durable decisions to
# your project's normal docs location before cleanup.
.vibe-tools/

# Agent-managed root files (provisioned per-machine by the CRL + vibe-tools
# extensions; they hold local paths / generated blocks, not project source).
/CLAUDE.md
/.mcp.json
# .claude/ holds per-machine provisioned files (settings.local.json, etc.) — ignored —
# EXCEPT skills/ and agents/, which are versioned CRL-domain project tooling
# (co-versioned with the grammar; see .claude/skills/encode-policy-to-crl, .claude/agents).
.claude/*
!.claude/skills/
!.claude/agents/
.cursor/


issues/

# Regression test output artifacts: regenerated on every test run; only
# the *-expected.* fixtures are committed baselines.
src/tests/regression/testdata/*-actual.*

# Feature working artifacts: local analysis corpora, derived data, and
# modeling scratch work. Kept local, not tracked in version control.
features/