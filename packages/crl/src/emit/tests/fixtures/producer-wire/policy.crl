// REFACTOR:grounded (#320, plan595): generic unary producer wiring, independent of explicit BMI publication.
library "BMI Wire".

terminology "Clinical Obese VS":
- valueset is `http://example.org/vitals/ValueSet/obese`.
terminology "Synthetic Threshold VS":
- valueset is `http://example.org/vitals/ValueSet/threshold`.
terminology "Height VS":
- valueset is `http://example.org/vitals/ValueSet/height`.
terminology "Weight VS":
- valueset is `http://example.org/vitals/ValueSet/weight`.

concept "Height":
- shape is Record.
- type is Observation.
- value type is Quantity.
- code is `height`.
- definition is most recent this.
- source representation:
  - type is Observation.
  - coded from "Height VS".

concept "Weight":
- shape is Record.
- type is Observation.
- value type is Quantity.
- code is `weight`.
- definition is most recent this.
- source representation:
  - type is Observation.
  - coded from "Weight VS".

concept "Threshold":
- shape is Record.
- type is Observation.
- value type is boolean.
- code is `threshold`.
- definition is "Weight" at least 30 'kg', then most recent this.
- source representation:
  - type is Observation.
  - coded from "Synthetic Threshold VS".

concept "Obese":
- shape is Record.
- type is Observation.
- value type is boolean.
- code is `obese`.
- definition is "Weight" at least 30 'kg', then most recent this.
- source representation:
  - type is Observation.
  - coded from "Clinical Obese VS".

concept "Has Weight":
- shape is Scalar.
- type is Observation.
- value type is boolean.
- code is `has-weight`.
- defined as exists ("Weight").

activity "A":
- request CPGCommunicationRequest.
- with `APPROVED`.

decision "D":
first:
- when "Has Weight" then recommend activity "A".
