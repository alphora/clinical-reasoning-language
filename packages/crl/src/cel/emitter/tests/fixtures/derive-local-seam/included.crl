# Seam Included — code-bearing concept library (BARE domain: primary via include).
library "Seam Included".

/* Pulled into the covers-target's include-walk seed via `include` in root.crl, so
   its local domain is the BARE <policyId> — NOT disambiguated. A sole-primary
   fallback (rejected, disc 490 [critical]) would wrongly disambiguate it, so this
   library is the regression guard for that decision. */

concept "Included Finding":
- type is Observation.
- value type is boolean.
- code is `included-finding`.
- definition is exists this.
