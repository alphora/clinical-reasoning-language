# cross-lib-activity-collision — Lib B1 (first shared disposition library).
library "SharedA".

// Defines an activity named `Deny`. SharedB defines one of the SAME name.
activity "Deny":
- request CPGCommunicationRequest.
- with `Denied by A.`.
