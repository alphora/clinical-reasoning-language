# sub-only — Other, a delegated sub-decision; the ONLY lib that recommends Shared.
library "Other".

concept "C2":
- type is Condition.
- value type is boolean.
- code is `c2`.
- definition is exists this.

decision "Other Decision":
first:
- when "C2" then recommend activity "Shared"."Deny".
- otherwise then recommend activity "Shared"."Deny".
