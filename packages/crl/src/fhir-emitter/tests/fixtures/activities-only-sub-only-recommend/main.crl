# sub-only — Main is the GRAPH-ROOT (delegates OUT to Other; does NOT recommend Shared).
library "Main".

concept "C1":
- type is Condition.
- value type is boolean.
- code is `c1`.
- definition is exists this.

decision "Main Decision":
first:
- when "C1" then use decision "Other"."Other Decision".
- otherwise then use decision "Other"."Other Decision".
