// package Foo — declares X as boolean (the WRONG type for a value comparison). include "Foo" binds
// package-first, so "Foo"."X" is THIS boolean X -> rule B must read boolean and fire one mismatch.
library "Foo".

concept "X":
- value type is boolean.
- type is Observation.
- code is `x`.
