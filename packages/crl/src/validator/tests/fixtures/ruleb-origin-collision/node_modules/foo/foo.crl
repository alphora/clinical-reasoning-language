// package Foo — declares X as Quantity (the RIGHT type for a value comparison). include "Foo"
// in root binds package-first, so "Foo"."X" is THIS X.
library "Foo".

concept "X":
- value type is Quantity.
- type is Observation.
- code is `x`.
