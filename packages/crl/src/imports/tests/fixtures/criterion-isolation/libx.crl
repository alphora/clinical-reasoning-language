# criterion-isolation LibX: a criterion "A" with no cross-library ref.
library "LibX".

concept "Age Qualifies":
- type is Observation.
- value type is boolean.
- code is `age`.

criterion "A":
- when ( "Age Qualifies" ).
