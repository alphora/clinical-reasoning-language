# criterion-isolation LibY: a DIFFERENT criterion "A"; same name, different library — must NOT close a cycle.
library "LibY".

concept "Age Qualifies":
- type is Observation.
- code is `age`.

criterion "A":
- when ( "Age Qualifies" ).
