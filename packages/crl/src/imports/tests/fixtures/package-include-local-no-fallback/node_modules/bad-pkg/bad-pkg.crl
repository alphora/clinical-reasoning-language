# BadPkg includes ProjectOnly — must NOT resolve to the consumer's local ProjectOnly
library "BadPkg".
include "ProjectOnly".

concept "Pkg C":
- type is Encounter.
- value type is CodeableConcept.
- coded from "Pkg T".

terminology "Pkg T":
- valueset is `urn:pkg-vs`.
