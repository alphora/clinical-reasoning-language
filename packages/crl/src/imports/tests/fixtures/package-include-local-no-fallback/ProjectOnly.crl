# local library ProjectOnly — should NOT satisfy BadPkg's include
library "ProjectOnly".

concept "Local C":
- type is Encounter.
- value type is CodeableConcept.
- coded from "Local T".

terminology "Local T":
- valueset is `urn:local-vs`.
