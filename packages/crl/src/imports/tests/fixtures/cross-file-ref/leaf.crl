# cross-file-ref leaf
library "Leaf".

// Single-layer (Asserted only): the terminology lives in a separate local
// vocab library ("Leaf Vocab") so `Leaf` itself is one layer and the
// layeredEmit gate leaves it as a single `Leaf` CQL library — which is what
// this cross-FILE-ref test exercises. Local siblings auto-resolve via the
// qualified ref (no `include` needed; cf. the cross-kind-same-name fixture).
concept "Leaf Concept":
- type is Encounter.
- value type is CodeableConcept.
- coded from "Leaf Vocab"."Leaf Vs".
