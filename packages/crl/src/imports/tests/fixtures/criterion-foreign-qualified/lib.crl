# criterion-foreign-qualified — the criterion lives in "Lib".
library "Lib".

concept "Age Qualifies":
- type is Observation.
- value type is boolean.
- code is `age`.

criterion "Eligible":
- when ( "Age Qualifies" ).
