export function mapPlanDefinitionToDecision(instance: any): string {
  // TODO: Implement mapping from PlanDefinition FSH instance to CPG-L decision block
  return `// [DEBUGGING] PlanDefinition: ${instance.name}\n`;
} 